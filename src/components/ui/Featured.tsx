import { ProductCardProps } from "../../types/productCardProps";

const Featured: React.FC<ProductCardProps> = ({ product }) => {
  console.log(product);
  return <div></div>;
};

export default Featured;
